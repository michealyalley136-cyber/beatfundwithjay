#
# BeatFund environment variables (example)
#
# Note: this repo's tooling filters dotfiles, so use this as your template and
# copy it to a local ".env" file (do NOT commit real secrets).
#

# Flask
FLASK_ENV=development
APP_BASE_URL=http://localhost:5000

# Feature flags
# KYC_ENFORCE=0 for dev/staging bypass; set to 1 in production.
KYC_ENFORCE=0

# Notifications polling interval (seconds)
NOTIF_POLL_SECONDS=30

# Affiliate program contact email
AFFILIATE_CONTACT_EMAIL=support@beatfund.com

# Security - REQUIRED for sessions and CSRF protection
# Generate a strong secret key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# Database (Postgres or SQLite)
# For Postgres (production):
# DATABASE_URL=postgresql://username:password@host:5432/dbname?sslmode=require
#
# For local SQLite (development, leave DATABASE_URL empty or unset):
# DATABASE_URL=
DATABASE_URL=

# Safe Postgres auto-migrations (idempotent, add-only)
PG_AUTO_MIGRATE=1

# Stripe (test mode)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
CONNECT_ONBOARDING_RETURN_URL=
CONNECT_ONBOARDING_REFRESH_URL=

# Stripe payout + booking fees
STRIPE_PLATFORM_FEE_PCT=0.02
STRIPE_BOOKING_HOLD_FEE=5.17

# Affiliate commissions
AFFILIATE_DEFAULT_PCT=0.30
AFFILIATE_PAYOUT_DELAY_DAYS=14

# BeatFund booking fees
# Legacy fee settings (kept for backward compatibility)
BEATFUND_FEE_RATE=0.02
BEATFUND_FEE_MIN_CENTS=517
BEATFUND_FEE_CAP_CENTS=0
STRIPE_PROC_RATE=0.029
STRIPE_PROC_FIXED_CENTS=30
ALLOW_REFUND_BF_FEE_ON_PROVIDER_FAULT=1

# Owner panel (superadmin)
# Recommended for local dev (used only when FLASK_ENV=development)
DEV_OWNER_PANEL_PASS=dev_owner_pass_change_me_123
